[project]
name = "aws_data_project"
requires-python = ">=3.12,<3.15"
version = "0.1.0"
dependencies = [
    "dbt-core==1.11.7",
    "dbt-snowflake==1.11.3",
    "apache-airflow[celery]==3.0.6 "
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"



